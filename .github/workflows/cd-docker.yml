name: CD - Docker Build & Deploy

on:
  push:
    tags:
      - "v*.*.*"     # build khi tag version (ví dụ v1.2.3)
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}   # ghcr.io/<owner>/<repo>
  SERVICES: "auth product order api-gateway"

jobs:
  build-and-push:
    name: Build & Push images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # cần cho GHCR
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push per service
        shell: bash
        run: |
          set -e
          VERSION="${GITHUB_REF_NAME:-latest}"
          for SVC in $SERVICES; do
            CONTEXT="$SVC"
            IMAGE="${REGISTRY}/${IMAGE_PREFIX}/${SVC}"
            echo "🛠 Building ${IMAGE}:${VERSION}"
            docker buildx build \
              --platform linux/amd64 \
              --file "${CONTEXT}/Dockerfile" \
              --tag  "${IMAGE}:${VERSION}" \
              --tag  "${IMAGE}:latest" \
              --push \
              "${CONTEXT}"
          done

  deploy:
    name: Deploy via SSH (docker compose)
    needs: build-and-push
    if: ${{ github.ref_type == 'tag' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: SSH deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script_stop: true
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH }}   # thư mục trên server chứa docker-compose.yml

            # Xuất biến môi trường cho compose nếu cần (ví dụ JWT_SECRET, MONGO URIs)
            # echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" > .env

            # Kéo image mới & restart dịch vụ
            docker compose pull
            docker compose up -d

            # Kiểm tra nhanh health của gateway (nếu service đã có healthcheck HTTP)
            # chờ tối đa ~90s để các service healthy
            for i in {1..30}; do
              if curl -fsS http://localhost:3003 >/dev/null 2>&1; then
                echo "✅ Gateway is up."
                break
              else
                echo "⏳ Waiting gateway... ($i/30)"
                sleep 3
              fi
            done

            docker image prune -f
